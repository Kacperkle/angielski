<Window x:Class="angielski.Lista"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:angielski"
        mc:Ignorable="d"
        Title="Lista" Height="450" Width="800">
    <Grid Background="#FF2D2B2B">
        <Grid.RowDefinitions>
            <RowDefinition Height="0.5*"/>
            <RowDefinition Height="4*"/>
            <RowDefinition Height="0.5*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Button Content="Wróć" HorizontalAlignment="Center" Margin="0,144,0,0" Grid.Row="1" Grid.Column="0" VerticalAlignment="Top" Height="51" Width="138" Click="Wroc"/>
        <ScrollViewer Grid.Row="1" Grid.Column="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                      HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#FFFEFE">
            <TextBlock Name="txtBlockContent" TextWrapping="Wrap" FontSize="14" Text=""/>
        </ScrollViewer>

    </Grid>
</Window>